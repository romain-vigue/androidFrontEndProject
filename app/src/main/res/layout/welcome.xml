<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".GitkitDemo" >

    <Button
        android:id="@+id/sign_in"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/button_bg"
        android:text="@string/sign_in_label"
        android:textColor="@android:color/white" />

    <Button
        android:id="@+id/sign_up_patient"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/button_bg_sign_up"
        android:text="@string/sign_up_patient_label"
        android:textColor="@android:color/white"/>

    <Button
        android:id="@+id/sign_up_doctor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/button_bg_sign_up"
        android:text="@string/sign_up_doctor_label"
        android:textColor="@android:color/white" />

</LinearLayout>
